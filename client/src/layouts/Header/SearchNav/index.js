import { Container, Test } from "./styled";

const SearchNav = () => {
  return (
    <Container>
      <Test>
        <h3>벼락장터 / Search Section / 판매하기 / 내상점 / 번개톡 </h3>
      </Test>
    </Container>
  );
};

export default SearchNav;
